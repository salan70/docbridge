name: speclink_dart_scanner
description: SpecLink Dart scanner worker. Speaks the stdin/stdout JSON worker protocol.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  analyzer: ">=6.5.0 <10.0.0"

dev_dependencies:
  lints: 6.1.0
  test: ^1.25.0
