# doc_file_not_found

The `@doc` annotation targets `docs/missing.md`, which does not exist in the
managed docs set, so SpecLink reports `doc_file_not_found` (error).

Run: `just check-fixture doc_file_not_found`
