# doc_anchor_not_found

The `@doc` annotation targets `docs/spec.md#missing-section`. The doc file
exists but has no heading with that anchor, so DocBridge reports
`doc_anchor_not_found` (error).

Run: `just check-fixture doc_anchor_not_found`
